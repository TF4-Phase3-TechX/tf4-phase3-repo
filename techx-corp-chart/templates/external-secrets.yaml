{{/* Alertmanager's Slack Secret is owned by platform-secrets in the GitOps repository.
     Keep it out of this chart so External Secrets has exactly one controller owner. */}}
{{- /* Intentionally renders no chart-managed ExternalSecret. */ -}}
