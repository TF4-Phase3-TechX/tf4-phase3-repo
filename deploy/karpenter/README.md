# Deprecated Karpenter manifests

Terraform in `infra/terraform/karpenter-nodepool.tf` is the only owner of `techx-general`.

These historical YAML files are retained only for audit comparison. Do not apply them.
