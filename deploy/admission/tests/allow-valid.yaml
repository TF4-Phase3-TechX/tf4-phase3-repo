apiVersion: apps/v1
kind: Deployment
metadata:
  name: admission-test-valid
spec:
  replicas: 1
  selector:
    matchLabels: { app: admission-test-valid }
  template:
    metadata:
      labels: { app: admission-test-valid }
    spec:
      containers:
        - name: test
          image: busybox:1.37.0
          command: ["sh", "-c", "sleep 3600"]
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
            seccompProfile: { type: RuntimeDefault }
          resources:
            requests: { cpu: 5m, memory: 8Mi }
            limits: { cpu: 25m, memory: 32Mi }
