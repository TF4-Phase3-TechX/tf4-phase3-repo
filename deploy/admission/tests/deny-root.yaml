apiVersion: apps/v1
kind: Deployment
metadata:
  name: admission-test-root
spec:
  replicas: 1
  selector:
    matchLabels: { app: admission-test-root }
  template:
    metadata:
      labels: { app: admission-test-root }
    spec:
      containers:
        - name: test
          image: busybox:1.37.0
          command: ["sh", "-c", "sleep 3600"]
          securityContext:
            runAsNonRoot: false
            allowPrivilegeEscalation: false
            capabilities: { drop: ["ALL"] }
          resources:
            requests: { cpu: 5m, memory: 8Mi }
            limits: { cpu: 25m, memory: 32Mi }
