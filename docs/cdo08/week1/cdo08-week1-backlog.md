# CDO08 Week 1 Backlog

**Owner:** Hai / CDO08 PM  
**Scope:** Security + Reliability findings from Week 1 scan  
**Last updated:** 2026-07-10  
**Source evidence:**

- `docs/evidence/epic-07-security/security-scan-findings.md`
- `docs/evidence/epic-08-reliability/reliability-scan-findings.md`

## Purpose

Backlog này tổng hợp các findings CDO08 đã scan trong Week 1 để đưa vào Jira tổng và dùng làm input cho kế hoạch Week 2-3.

Week 1 chưa phải là giai đoạn sửa toàn bộ vấn đề. Mục tiêu hiện tại là:

1. Ghi nhận rủi ro Security/Reliability có evidence rõ.
2. Chuẩn hóa từng rủi ro thành backlog item có owner, impact, evidence, cách kiểm tra và rollback note.
3. Dùng rubric ưu tiên để chọn việc nào nên làm trước trong Week 2-3.

---

## Epic Mapping

| Epic    | Pillar      | Evidence doc                                                     |
| ------- | ----------- | ---------------------------------------------------------------- |
| Epic 07 | Security    | `docs/evidence/epic-07-security/security-scan-findings.md`       |
| Epic 08 | Reliability | `docs/evidence/epic-08-reliability/reliability-scan-findings.md` |

> **Ghi chú ID:** `CDO08-REL-xx` = Reliability (Epic 08) · `CDO08-SEC-xx` = Security (Epic 07)

---

## Current Runtime Summary

Scan time gần nhất: `2026-07-09 09:31 ICT`.

Kết quả runtime chính:

- App pods trong namespace `techx-tf4` đang `Running 1/1`.
- Public ALB frontend trả `HTTP/1.1 200 OK`.
- Namespace `techx-tf4` không có PVC.
- Namespace `techx-tf4` không có PodDisruptionBudget.
- `flagd` đang chạy nhưng evidence của Thuỷ cho thấy central sync bị vô hiệu hóa/comment và runtime đọc flag từ file local.

---

## Backlog Items (Sorted by Priority & Score - Business Centric)

| ID           | Priority | Owner chính   | Finding                                                                                     | Tác động Business & SLO (Chi tiết) &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; | Evidence                                                                                                                                                                                      | Giải pháp & Nhiệm vụ chi tiết (Khắc phục triệt để)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Verification                                                                                                           | Rollback / Safety note                                                                                                             | Coordination                                                                         |
| ------------ | -------- | ------------- | ------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| CDO08-REL-01 | **P0**   | Nam           | Critical services đều chỉ có `1 replica`                                                    | Khi Node EKS bị restart, bảo trì hoặc sập pod:<br>**1.** Gây gián đoạn hoàn toàn luồng mua sắm của khách hàng trong 1-3 phút (gây tổn thất doanh thu trực tiếp).<br>**2.** Trực tiếp làm vỡ cam kết SLO Checkout thành công ($\ge$ 99.0%) và SLO Giỏ hàng ($\ge$ 99.5%).<br>**3.** Tạo ra điểm nghẽn đơn lẻ (SPOF) trên hạ tầng.                                                                                                                                                                                                                                                                                                                                                    | `kubectl -n techx-tf4 get deploy`; `techx-corp-chart/values.yaml:27` default `replicas: 1`                                                                                                    | 1. Cập nhật `values.yaml`: đặt `replicas: 2` cho các service: `frontend-proxy`, `frontend`, `checkout`, `cart`, `payment`, `shipping`, `product-catalog`.<br>2. Thêm cấu hình `podAntiAffinity` kiểu `requiredDuringSchedulingIgnoredDuringExecution` theo `hostname` để đảm bảo 2 replicas luôn chạy trên 2 node khác nhau.<br>3. Verify không có node đang chạy quá 1 replica của cùng service bằng `kubectl -n techx-tf4 get pods -o wide`.                                                                                            | `kubectl -n techx-tf4 get deploy` sau thay đổi phải thấy replicas tăng và pods ready                                   | Rollback replica count về 1 nếu cost/resource hoặc scheduling lỗi                                                                  | CDO04 review cost; Nguyên review technical risk                                      |
| CDO08-REL-02 | **P0**   | Nam           | App pods chưa cấu hình readiness/liveness probe                                             | Khi deploy phiên bản mới (rollout) hoặc pod bị restart:<br>**1.** Kubernetes tiếp tục định tuyến traffic của khách vào pod mới chưa khởi chạy xong, gây ra hàng loạt lỗi kết nối HTTP 5xx (INC-3).<br>**2.** Trực tiếp làm sụt giảm SLO Checkout và Giỏ hàng.<br>**3.** Pod bị treo/deadlock không được phát hiện tự động để restart, kéo dài thời gian MTTR.                                                                                                                                                                                                                                                                                                                       | Runtime jsonpath readiness/liveness trả rỗng; `techx-corp-chart/values.yaml:152-153` chỉ có probe dạng comment                                                                                | 1. Xác định cổng health check (gRPC health protocol cho Go/C#, HTTP check cho NodeJS/Python) của từng service.<br>2. Bật cấu hình `readinessProbe` và `livenessProbe` trong `values.yaml` của chart cho các service trên checkout path.<br>3. Thiết lập thông số: `initialDelaySeconds: 15`, `periodSeconds: 10`, `timeoutSeconds: 3`, `failureThreshold: 3`.                                                                                                                                                                             | `kubectl -n techx-tf4 describe pod` hoặc jsonpath phải thấy readiness/liveness configured; rollout không làm pod stuck | Rollback probe config nếu probe sai làm pod không ready                                                                            | Nguyên review; Quân validate checkout smoke test                                     |
| CDO08-REL-09 | **P0**   | Quân          | Checkout path thiếu timeout/deadline/retry cho many dependency sync                         | Khi một dependency downstream (payment, shipping, v.v.) phản hồi chậm:<br>**1.** Luồng gọi đồng bộ bị block khiến checkout request bị treo, làm cạn kiệt worker threads/goroutines của checkout service.<br>**2.** Gây sập lan truyền (cascading failure) toàn hệ thống và vỡ SLO p95 latency storefront (< 1s).<br>**3.** Khách hàng bấm thanh toán liên tục do trang bị đơ → rủi ro trùng lặp giao dịch (double charge).                                                                                                                                                                                                                                                          | Quân report F01-F04/G1-G7/G10: `checkout/main.go` dùng gRPC/HTTP call không có per-call timeout; `shipping/quote.rs` dùng `awc::Client::new()`                                                | 1. Cấu hình gRPC client trong `checkout/main.go` dùng `context.WithTimeout` cho mỗi dependency call (Cart: 200ms, Catalog: 150ms, Payment: 1.5s, Shipping: 500ms).<br>2. Thêm retry với exponential backoff (tối đa 3 lần) cho các API idempotent.<br>3. Áp dụng fallback mặc định cho các service phụ (email, recommendation) để tránh sập checkout chính.                                                                                                                                                                               | Fault test dependency chậm phải fail bounded theo timeout thay vì treo request                                         | Rollback từng service timeout nếu gây false failure; không rollout global nếu chưa smoke test checkout                             | Nguyên review; Quyết hỗ trợ SLO query                                                |
| CDO08-REL-03 | **P0**   | Phương        | PostgreSQL, Valkey, Kafka single replica và namespace app không có PVC                      | Khi các pod stateful này bị restart, sập node hoặc reschedule:<br>**1.** Dữ liệu giỏ hàng đang hoạt động của khách hàng bị mất sạch trong Valkey (giảm tỷ lệ conversion checkout).<br>**2.** Cơ sở dữ liệu PostgreSQL bị mất sạch dữ liệu đơn hàng và danh mục sản phẩm.<br>**3.** Các event đơn hàng chưa được consume trong Kafka bị xóa sạch, gây mất đồng bộ dữ liệu giao dịch và hỏng luồng đối soát kế toán.                                                                                                                                                                                                                                                                  | `kubectl -n techx-tf4 get pvc` không có resources; `postgresql`, `valkey-cart`, `kafka` replicas `1`                                                                                          | 1. Chuyển đổi workloads `postgresql`, `valkey-cart`, `kafka` sang **StatefulSet** và bổ sung `volumeClaimTemplates` với StorageClass `gp3` (EBS) — dung lượng đề xuất: PostgreSQL 20Gi, Valkey 5Gi, Kafka 20Gi/broker.<br>2. Tăng replicas: Kafka lên 3 brokers (`replicationFactor: 3`), Valkey lên 1 master + 2 replicas (Sentinel mode), PostgreSQL lên 1 primary + 1 standby (streaming replication).<br>3. Verify dữ liệu còn nguyên sau khi xóa pod thủ công: `kubectl delete pod valkey-cart-0` → kiểm tra cart data vẫn hiển thị. | Evidence bằng `kubectl get pvc`, chart values, và restore/backup plan nếu có                                           | Không bật persistence/HA nếu chưa có migration/rollback plan                                                                       | CDO04 review cost nếu dùng HA/managed service                                        |
| CDO08-REL-07 | **P0**   | Quân / Nguyên | Cấu hình Kafka Producer bất đồng bộ dễ gây mất tin nhắn đơn hàng thầm lặng                  | Khi Kafka broker sập/quá tải hoặc nghẽn mạng:<br>**1.** Các event đơn hàng bị mất thầm lặng (silently lost) mà không báo lỗi.<br>**2.** Gây mất đồng bộ dữ liệu nghiêm trọng: Khách hàng đã bị trừ tiền thành công từ service `payment` nhưng service `accounting` hoàn toàn không nhận được đơn hàng (gây khiếu nại lớn, tốn chi phí đối soát thủ công và ảnh hưởng uy tín thương hiệu).                                                                                                                                                                                                                                                                                           | `techx-corp-platform/src/checkout/kafka/producer.go`: config `RequiredAcks = sarama.NoResponse` và `sarama.NewAsyncProducer` không đọc channel `Errors()`                                     | 1. Sửa `checkout/kafka/producer.go`: Chuyển sang `sarama.NewSyncProducer` để gửi đồng bộ.<br>2. Cấu hình: `Producer.RequiredAcks = sarama.WaitForAll` (broker lead và replicas lưu thành công mới ACK) và đặt `Producer.Retry.Max = 5`.<br>3. Áp dụng **Transactional Outbox Pattern**: Khi checkout thành công, ghi event vào bảng `outbox_events` trong cùng transaction DB với order. Một worker riêng đọc bảng này và publish lên Kafka — đảm bảo at-least-once delivery và không mất event kể cả khi Kafka tạm thời sập.             | Giả lập sập Kafka broker, chạy checkout và xác nhận lỗi được ghi nhận thay vì trôi qua thầm lặng                       | Khôi phục cấu hình Async Producer và `RequiredAcks = sarama.NoResponse` nếu có lỗi nghẽn hiệu năng                                 | CDO07 hỗ trợ cấu hình; Nguyên review technical risk; Phương review Kafka/data impact |
| CDO08-REL-10 | **P1**   | Nguyên / Quân | Payment khởi tạo OpenFeature provider trong mỗi request thanh toán                          | Dưới tải cao (ví dụ: flash sale):<br>**1.** Việc thiết lập lại kết nối gRPC/WebSocket tới flagd sidecar cho mỗi request thanh toán sẽ tạo ra bottleneck hiệu năng nghiêm trọng.<br>**2.** Làm tăng vọt p95 latency của cổng thanh toán, dẫn đến timeout giao dịch của khách hàng, trực tiếp đe dọa SLO Checkout success rate $\ge$ 99.0% và gây quá tải hệ thống.                                                                                                                                                                                                                                                                                                                   | `docs/cdo08/week1/nguyen-architecture-technical-risk-findings.md` CDO08-REL-03; `techx-corp-platform/src/payment/charge.js` gọi `OpenFeature.setProviderAndWait(flagProvider)` trong `charge` | 1. Sửa `payment/charge.js`: Xóa dòng `await OpenFeature.setProviderAndWait(flagProvider)` bên trong hàm `charge`.<br>2. Di chuyển phần khởi tạo provider vào file `index.js` (startup) của payment service, đảm bảo chỉ thực hiện kết nối 1 lần duy nhất lúc khởi động app.<br>3. Sử dụng client toàn cục đã được khởi tạo để đọc flag trong hàm `charge`.                                                                                                                                                                                | Load/smoke test payment cho thấy provider init chỉ chạy một lần và checkout vẫn đọc `paymentFailure` flag              | Rollback payment change nếu SDK init lifecycle gây service start lỗi                                                               | Thuỷ/flagd owner xác nhận flag behavior                                              |
| CDO08-REL-11 | **P1**   | Phương        | Chưa có backup/restore proof cho PostgreSQL, Valkey, Kafka                                  | Khi xảy ra thảm họa về dữ liệu (như data corruption, lỗi ổ đĩa hoặc thao tác sai):<br>**1.** Team vận hành hoàn toàn không có phương án và bằng chứng chứng minh khả năng khôi phục dữ liệu.<br>**2.** Vi phạm nghiêm trọng chỉ số RPO (mức độ mất mát dữ liệu tối đa) và RTO (thời gian khôi phục dịch vụ tối đa), đe dọa tính liên tục của hoạt động kinh doanh.                                                                                                                                                                                                                                                                                                                  | Phương `DR-003`, `DR-010`; hiện không thấy PVC/backup/restore runbook trong namespace app                                                                                                     | 1. Viết script tự động chạy cronjob chạy `pg_dump` cho PostgreSQL hằng ngày, và snapshot/export định kỳ cho Valkey/Kafka.<br>2. Viết tài liệu runbook DR-003, DR-010 hướng dẫn khôi phục dữ liệu chi tiết.<br>3. Thực hiện diễn tập khôi phục (restore test) định kỳ trên môi trường staging.                                                                                                                                                                                                                                             | Có runbook, evidence backup location, và kết quả restore test hoặc documented blocker                                  | Không thay đổi storage mode nếu chưa có migration/restore plan                                                                     | CDO04 review cost/storage; Nguyên review risk                                        |
| CDO08-SEC-05 | **P1**   | Nhân          | Internet-facing HTTP ingress/frontend-proxy có route target admin/observability/flagd-ui    | Toàn bộ bề mặt tấn công quản trị (admin dashboards) bị phơi bày ra internet:<br>**1.** Kẻ tấn công có thể truy cập nặc danh vào Jaeger/Grafana để đọc cấu hình hệ thống, trace logs chứa thông tin nhạy cảm của khách hàng (PII).<br>**2.** Nguy cơ bị can thiệp vào giao diện flagd-ui để tự ý bật các flag lỗi (fault injection), làm sập hoặc làm sai lệch hành vi của luồng checkout ngay lập tức mà không cần xác thực.                                                                                                                                                                                                                                                        | Nhân `SEC-PLAT-002`; `deploy/ingress.yaml:16-20`; `frontend-proxy/envoy.tmpl.yaml:40-61`, `197-244`                                                                                           | 1. Tạo `ingress-internal.yaml` riêng với annotation `kubernetes.io/ingress.class: alb` và `alb.ingress.kubernetes.io/scheme: internal` — chỉ route `/grafana`, `/jaeger`, `/loadgen`, `/feature` vào internal ALB (chỉ accessible từ VPC/VPN).<br>2. Xóa các route admin ra khỏi `ingress.yaml` (public ALB) và `frontend-proxy/envoy.tmpl.yaml`.<br>3. Test từ public internet xác nhận các route admin trả `404` hoặc `connection refused`.                                                                                             | Route validation chứng minh `/grafana`, `/jaeger`, `/loadgen`, `/feature` không public hoặc có auth                    | Rollback proxy route change nếu demo flow cần, nhưng không expose admin public mặc định                                            | Quyết/observability owner; Deploy Operator                                           |
| CDO08-SEC-01 | **P1**   | Thủy          | Hardcoded DB credentials/API key trong Helm values                                          | Rò rỉ thông tin mật:<br>**1.** Mật khẩu kết nối CSDL và API keys được lưu dưới dạng plain text trong Git repository, bất kỳ ai có quyền đọc repo đều có thể lấy được.<br>**2.** Cho phép kẻ tấn công kết nối trực tiếp vào database nội bộ của sản xuất để đánh cắp thông tin khách hàng, thay đổi giá sản phẩm hoặc xóa sạch dữ liệu.                                                                                                                                                                                                                                                                                                                                              | `techx-corp-chart/values.yaml:182-183`, `581-582`, `618-619`, `600-601`, `870-871`                                                                                                            | 1. Tạo các Kubernetes Secrets (`postgresql-secret`, `app-secrets`) chứa credentials dạng mã hóa base64.<br>2. Sửa `values.yaml` và templates: Xóa bỏ hoàn toàn credentials plain-text.<br>3. Cấu hình map secret vào pod thông qua `env.valueFrom.secretKeyRef` trong file deployment template của các microservice (`checkout`, `product-catalog`, `product-reviews`, `accounting`).                                                                                                                                                     | Secret không còn hardcoded trong values; service vẫn start sau deploy                                                  | Rollback về values cũ nếu service không đọc được secret mới                                                                        | Nhân review security; Deploy Operator hỗ trợ deploy                                  |
| CDO08-SEC-02 | **P1**   | Nhân / Quyết  | Grafana anonymous user có quyền Admin nếu Grafana được expose lại                           | Khi Grafana bị truy cập tự do từ bên ngoài qua ALB:<br>**1.** Bất kỳ người dùng ẩn danh nào cũng có quyền tối cao (Admin) trên hệ thống giám sát.<br>**2.** Họ có thể thay đổi/xóa dashboard, tắt các cảnh báo Alertmanager, hoặc trích xuất mật khẩu của các datasource kết nối với database sản xuất, gây mất kiểm soát vận hành hoàn toàn.                                                                                                                                                                                                                                                                                                                                       | `techx-corp-chart/values.yaml:1190-1193` anonymous enabled và `org_role: Admin`; `1197` admin password `admin`                                                                                | 1. Sửa cấu hình Grafana trong `values.yaml` (dòng 1188-1193): Đặt `org_role: Viewer` để chặn anonymous Admin.<br>2. Đặt `disable_login_form: false` để bật lại màn hình login.<br>3. Tạo mật khẩu admin mạnh thông qua Kubernetes Secret thay cho password mặc định `admin`.                                                                                                                                                                                                                                                              | Grafana không còn anonymous Admin; login/access policy rõ ràng                                                         | Rollback access config nếu team mất access, nhưng không rollback về anonymous Admin nếu public                                     | Quyết xác nhận observability; Deploy Operator nếu cần deploy                         |
| CDO08-REL-06 | **P1**   | Quyết         | Alertmanager disabled                                                                       | Khi hệ thống gặp sự cố (như checkout bị fail hàng loạt hoặc database sập):<br>**1.** Đội ngũ vận hành hoàn toàn "bị mù" thông tin, không nhận được bất kỳ cảnh báo tự động nào.<br>**2.** Thời gian phát hiện sự cố (MTTD) và thời gian xử lý sự cố (MTTR) bị kéo dài do phụ thuộc hoàn toàn vào việc kiểm tra thủ công, làm vỡ nát error budget của SLO.                                                                                                                                                                                                                                                                                                                           | `techx-corp-chart/values.yaml:1118-1121` có `alertmanager.enabled: false`                                                                                                                     | 1. Bật `alertmanager.enabled: true` trong `values.yaml` của Prometheus.<br>2. Cấu hình receiver webhook gửi cảnh báo trực tiếp về Slack/Discord/Email của team.<br>3. Thiết lập các alert rule quan trọng: `CheckoutErrorRateHigh` (error > 1%), `CheckoutLatencyP95High` (latency > 1s), `ValkeyCartDown`, `PostgreSqlDown`.                                                                                                                                                                                                             | Alert rule load được và có test firing hoặc screenshot Grafana/Prometheus                                              | Rollback alert rules nếu noise quá cao                                                                                             | Nam/Phương cung cấp runtime/data signals                                             |
| CDO08-REL-12 | **P2**   | Nam           | Revenue path chưa có PodDisruptionBudget                                                    | Khi hạ tầng EKS tự động nâng cấp node hoặc thực hiện bảo trì định kỳ:<br>**1.** Các pod của các service critical có thể bị trục xuất (evicted) đồng thời, gây ra downtime ngắn cho luồng checkout.<br>**2.** Gây lỗi 5xx đột ngột làm sụt giảm chỉ số SLO thành công của giỏ hàng và thanh toán.                                                                                                                                                                                                                                                                                                                                                                                    | `kubectl -n techx-tf4 get pdb` trả `No resources found`; Nam `NAM-RUNTIME-007`                                                                                                                | 1. Chỉ thực hiện sau khi tăng replicas các service lên >= 2.<br>2. Viết file template `pdb.yaml` trong Helm chart cho các service: `checkout`, `cart`, `payment`, `frontend`.<br>3. Cấu hình `minAvailable: 1` trong PDB để đảm bảo EKS node drain luôn giữ ít nhất 1 pod healthy — không drain node nếu service chỉ còn 1 replica.                                                                                                                                                                                                       | `kubectl get pdb` có minAvailable phù hợp và rollout/node drain không bị block sai                                     | Không tạo PDB cho single-replica workload gây kẹt node drain                                                                       | CDO04 cost; Nguyên review                                                            |
| CDO08-SEC-06 | **P2**   | Nhân / Quyết  | Grafana ClusterRole có quyền đọc Secrets toàn cluster trong rendered observability manifest | Nếu pod Grafana bị tấn công hoặc cấu hình sai plugin:<br>**1.** Kẻ tấn công có thể lợi dụng token của ServiceAccount này để đọc toàn bộ các Kubernetes Secrets trong mọi namespace khác trên cụm EKS (bao gồm cả database password thật, AWS keys).<br>**2.** Mở rộng blast radius từ một công cụ giám sát sang toàn bộ hạ tầng bảo mật của doanh nghiệp.                                                                                                                                                                                                                                                                                                                           | Nhân `SEC-PLAT-004`; rendered `grafana-clusterrole` có `resources: [configmaps, secrets]`, `verbs: [get, watch, list]`                                                                        | 1. Sửa file manifest của Grafana ClusterRole: Giới hạn phạm vi (Namespace Role) chỉ được đọc Secrets trong namespace `techx-observability`.<br>2. Xóa quyền đọc Secret toàn cluster (ClusterRole) để giảm blast radius nếu Grafana bị chiếm quyền.                                                                                                                                                                                                                                                                                        | Rendered manifest không còn ClusterRole đọc Secrets toàn cluster; dashboard provisioning vẫn hoạt động                 | Rollback sang Role/ClusterRole cũ nếu dashboard provisioning fail, nhưng ghi rõ exception                                          | Quyết review dashboard provisioning                                                  |
| CDO08-SEC-03 | **P2**   | Nhân / Quyết  | OpenSearch security plugin disabled                                                         | Khi OpenSearch không yêu cầu xác thực:<br>**1.** Bất kỳ ai kết nối tới OpenSearch đều có thể đọc/ghi/xóa dữ liệu logs và traces.<br>**2.** Kẻ tấn công có thể xóa sạch dấu vết log audit để che giấu hành vi xâm nhập, hoặc thu thập logs chứa dữ liệu nhạy cảm (PII) của khách hàng được in ra trong luồng checkout.                                                                                                                                                                                                                                                                                                                                                               | `techx-corp-chart/values.yaml:1227-1230` có `DISABLE_SECURITY_PLUGIN=true`; Nhân `SEC-PLAT-003`                                                                                               | 1. Cấu hình `DISABLE_SECURITY_PLUGIN: "false"` trong `values.yaml` của OpenSearch.<br>2. Thiết lập Basic Authentication/TLS cho OpenSearch API để bảo vệ logs/traces.<br>3. Cập nhật thông tin credentials mới cho OpenTelemetry Collector và Jaeger để gửi telemetry có xác thực.                                                                                                                                                                                                                                                        | OpenSearch access path được kiểm chứng; security plugin/network policy được review                                     | Không bật security plugin trực tiếp nếu có nguy cơ làm ingestion/query lỗi                                                         | Quyết review log pipeline; Nguyên review technical risk                              |
| CDO08-SEC-04 | **P2**   | Nhân          | Container securityContext hardening chưa đồng đều                                           | Khi các container chạy với quyền root mặc định và không được hardening:<br>**1.** Nếu container bị chiếm quyền điều khiển (qua lỗi code RCE), kẻ tấn công có thể dễ dàng thoát khỏi container (container escape) để kiểm soát trực tiếp node vật lý EKS.<br>**2.** Thực hiện cài cắm mã độc đào coin hoặc quét mạng nội bộ để tấn công các service khác.                                                                                                                                                                                                                                                                                                                            | Nhân `SEC-PLAT-001`; `values.yaml:36` default securityContext rỗng; runtime chỉ 6 components có `runAsNonRoot`                                                                                | 1. Định nghĩa cấu hình `securityContext` chung ở tầng chart.<br>2. Cấu hình `securityContext`: `runAsNonRoot: true`, `runAsUser: 10001`, `allowPrivilegeEscalation: false`, `readOnlyRootFilesystem: true` — đồng thời mount `emptyDir` vào `/tmp` và các thư mục app cần ghi để tránh crash do readonly filesystem.<br>3. Áp dụng dần cho từng microservice, thực hiện smoke test để tránh lỗi crash do phân quyền ghi file local.                                                                                                       | Runtime pod specs có securityContext; smoke test service pass                                                          | Không bật `readOnlyRootFilesystem` global trước khi test write path từng image                                                     | Nguyên review; service owner smoke test                                              |
| CDO08-REL-05 | **P2**   | Quyết         | Prometheus/OpenSearch persistence disabled                                                  | Khi các pod Prometheus/OpenSearch bị restart hoặc reschedule:<br>**1.** Toàn bộ dữ liệu log lịch sử điều tra sự cố và metrics đo đạc SLO dài hạn của dự án bị xóa sạch.<br>**2.** Đội ngũ vận hành không thể đối soát, phân tích nguyên nhân gốc rễ (Root Cause Analysis - RCA) của các sự cố đã qua để lập báo cáo Postmortem.                                                                                                                                                                                                                                                                                                                                                     | `techx-corp-chart/values.yaml:1174-1175` Prometheus PV disabled; `1222-1223` OpenSearch persistence disabled                                                                                  | 1. Bật `persistentVolume.enabled: true` cho Prometheus Server và OpenSearch.<br>2. Cấu hình dung lượng ổ đĩa phù hợp (ví dụ: 20Gi cho Prometheus, 50Gi cho OpenSearch) sử dụng EBS `gp3` StorageClass.<br>3. Thiết lập chính sách retention period (Prometheus giữ metric trong 7 ngày, OpenSearch giữ log trong 5 ngày) để tối ưu chi phí lưu trữ trong EKS.                                                                                                                                                                             | Restart test hoặc config review chứng minh metric/log retention đáp ứng yêu cầu                                        | Không bật PVC nếu storage class/cost chưa được review                                                                              | CDO04 review storage cost                                                            |
| CDO08-REL-08 | **P2**   | Thuỷ / Nam    | `flagd` central sync đang bị vô hiệu hóa và runtime đọc flag từ file local                  | Khi BTC thay đổi kịch bản thử nghiệm hoặc cấu hình fault injection từ hệ thống trung tâm:<br>**1.** Team CDO08 hoàn toàn không nhận được cấu hình mới, dẫn đến việc chạy thử kịch bản bị sai lệch, không có log đồng bộ chứng minh sự tuân thủ quy chế với BTC.<br>**2.** Có nguy cơ bị phạt điểm hoặc truất quyền thi đấu (DQ) do vi phạm quy chế bảo vệ control-plane của BTC.                                                                                                                                                                                                                                                                                                    | `deploy/values-flagd-sync.yaml:10-23` sync command/token đang comment; log `flagd` đọc `./etc/flagd/demo.flagd.json`; Secret `flagd-sync` là `placeholder`                                    | 1. Mở comment và điền token thật cấu hình URL sync trong file `deploy/values-flagd-sync.yaml`.<br>2. Loại bỏ shell wrapper khởi chạy flagd cũ, cấu hình flagd chạy trực tiếp qua Helm values.<br>3. Đảm bảo pod đồng bộ thành công với central server của BTC trước khi chạy test.                                                                                                                                                                                                                                                        | `kubectl logs` phải thấy sync từ central provider thành công; các service có `FLAGD_HOST=flagd` vẫn running            | Không bật lại command cũ dùng `/bin/sh -c`; rollback về bản local only nếu sync fix làm `flagd` crash, nhưng phải ghi rõ rule risk | Thuỷ owner config; Nam deploy/runbook; Nguyên review                                 |
| CDO08-SEC-07 | **P2**   | Nhân          | App workloads dùng chung ServiceAccount `techx-corp`                                        | Khi mọi workload dùng chung một ServiceAccount và SA này được bind thêm quyền hạn trong tương lai:<br>**1.** Tất cả các pod (kể cả những pod không critical như ad, recommendation) đều tự động nhận các quyền hạn cao này.<br>**2.** Một pod phụ bị thỏa hiệp sẽ kéo theo sự compromise của toàn bộ các service khác trên EKS, vi phạm nguyên lý least privilege.                                                                                                                                                                                                                                                                                                                  | Runtime deployment serviceAccountName đều `techx-corp`; Nhân `SEC-PLAT-005`                                                                                                                   | 1. Tạo các ServiceAccount riêng biệt cho từng service trong Helm templates (ví dụ: `techx-corp-checkout`, `techx-corp-cart`).<br>2. Thay thế ServiceAccount mặc định dùng chung.<br>3. Gán các quyền tối thiểu thông qua RBAC Role/RoleBinding thay vì gộp chung quyền.                                                                                                                                                                                                                                                                   | Service cần RBAC có SA riêng; các service khác không inherit quyền                                                     | Không refactor toàn bộ SA nếu chưa có test/deploy window                                                                           | Nguyên review chart impact                                                           |
| CDO08-SEC-08 | **P3**   | Nhân          | OTLP HTTP receiver CORS quá rộng                                                            | Khi allowed origins được đặt là wildcard `*`:<br>**1.** Kẻ tấn công có thể viết các đoạn script chạy trên browser của người dùng để gửi hàng loạt telemetry giả mạo/rác vào collector.<br>**2.** Gây quá tải bộ lưu trữ OpenSearch/Prometheus, làm nhiễu loạn dữ liệu tracing và biểu đồ giám sát SLO.                                                                                                                                                                                                                                                                                                                                                                              | `techx-corp-chart/values.yaml:940-944` allowed origins `http://*`, `https://*`; Nhân `SEC-PLAT-006`                                                                                           | 1. Sửa file cấu hình của OpenTelemetry Collector.<br>2. Thay thế CORS `allowed_origins: ["http://*", "https://*"]` bằng danh sách các domain storefront và dev origins được duyệt.<br>3. Chặn các origin nặc danh gửi rác telemetry vào collector.                                                                                                                                                                                                                                                                                        | OTel browser telemetry vẫn hoạt động với origin hợp lệ; origin ngoài bị chặn                                           | Rollback CORS nếu làm mất web traces, nhưng chỉ trong dev overlay                                                                  | Quyết/AIO xác nhận telemetry need                                                    |

---

## Rubric-Based Priority Assessment

Bảng chấm điểm chi tiết áp dụng công thức: **$\text{Priority Score} = (L \times S) \times B$**  
_Theo quy định của [pm-priority-rubric](pm-priority-rubric.md)._

> **Chú thích:** **L** = Likelihood (Khả năng xảy ra, 1–5) · **S** = Severity (Mức nghiêm trọng kỹ thuật, 1–5) · **B** = Business Impact (Tác động kinh doanh/SLO, 1–5)

| ID           |   L |   S |   B |  Score | Priority | Lý do đánh giá (Quy về Business & SLO)                                                                                                                                                                                                                                                                   | Cost / Perf flag                | Review status |
| ------------ | --: | --: | --: | -----: | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ------------- |
| CDO08-REL-01 |   4 |   4 |   5 | **80** | P0       | **L=4:** Pod restart/node drain xảy ra thường xuyên mỗi khi deploy hoặc EKS bảo trì.<br>**S=4:** Single replica đồng nghĩa với sập hoàn toàn service trong 1-3 phút không có failover.<br>**B=5:** Trực tiếp phá vỡ SLO Checkout ≥99.0% và Giỏ hàng ≥99.5%, mất doanh thu tức thì.                       | Needs CDO04 Review (Tăng cost). | Approved      |
| CDO08-REL-02 |   4 |   4 |   5 | **80** | P0       | **L=4:** Mỗi lần deploy/rollout đều trigger trạng thái pod chưa sẵn sàng nhận traffic.<br>**S=4:** Kubernetes route traffic vào pod chưa ready gây 5xx hàng loạt trong suốt quá trình rollout.<br>**B=5:** Vi phạm SLO checkout trực tiếp khi triển khai, pod deadlock không tự restart kéo dài MTTR.    | Low cost (Cần test threshold).  | Approved      |
| CDO08-REL-03 |   3 |   5 |   5 | **75** | P0       | **L=3:** Pod stateful bị restart ít thường xuyên hơn nhưng chắc chắn xảy ra khi node sập hoặc reschedule.<br>**S=5:** Không có PVC đồng nghĩa mất dữ liệu vĩnh viễn, không thể khôi phục.<br>**B=5:** Mất sạch đơn hàng, giỏ hàng, event Kafka = mất doanh thu hoàn toàn và hỏng luồng đối soát kế toán. | Needs CDO04 Review (Tăng cost). | Approved      |
| CDO08-REL-05 |   3 |   3 |   2 | **18** | P2       | **L=3:** Xảy ra mỗi lần pod Prometheus/OpenSearch bị restart.<br>**S=3:** Mất metrics/logs lịch sử nhưng app vẫn chạy bình thường.<br>**B=2:** Không ảnh hưởng đến giao dịch khách hàng, chỉ làm mù RCA khi cần điều tra sự cố.                                                                          | Needs CDO04 Review.             | Approved      |
| CDO08-REL-06 |   4 |   3 |   3 | **36** | P1       | **L=4:** Alertmanager đang tắt hoàn toàn — không có alert nào được gửi hiện tại.<br>**S=3:** Không crash hệ thống nhưng MTTD tăng cao khi có sự cố, team phụ thuộc vào check thủ công.<br>**B=3:** Error budget của SLO bị ăn mòn chậm do phát hiện sự cố muộn hơn đáng kể.                              | Low cost.                       | Approved      |
| CDO08-REL-07 |   3 |   4 |   5 | **60** | P0       | **L=3:** Xảy ra khi Kafka chịu tải cao hoặc có network issue ngắn.<br>**S=4:** Event đơn hàng mất thầm lặng, không có cảnh báo, không thể phát hiện ngay.<br>**B=5:** Khách bị trừ tiền từ `payment` nhưng `accounting` không nhận được đơn — khiếu nại trực tiếp, tốn chi phí đối soát thủ công.        | Low cost (Cần cấu hình lại).    | Approved      |
| CDO08-REL-08 |   3 |   2 |   2 | **12** | P2       | **L=3:** BTC có thể thay đổi flag config bất kỳ lúc nào trong quá trình thi.<br>**S=2:** App fallback sang local config, vẫn hoạt động bình thường với khách hàng.<br>**B=2:** Không ảnh hưởng giao dịch nhưng vi phạm quy chế control-plane của BTC, có nguy cơ bị phạt điểm.                           | Low cost.                       | Approved      |
| CDO08-REL-09 |   4 |   4 |   5 | **80** | P0       | **L=4:** Bất kỳ lúc nào dependency (payment, shipping) chậm hoặc quá tải đều trigger.<br>**S=4:** Goroutine bị block dây chuyền gây cascading failure toàn checkout service.<br>**B=5:** SLO latency p95 <1s bị vỡ trực tiếp; khách bấm lại nhiều lần sinh rủi ro double charge.                         | Low/Medium cost.                | Approved      |
| CDO08-REL-10 |   4 |   3 |   4 | **48** | P1       | **L=4:** Mỗi request payment đều gọi lại `setProviderAndWait` — trigger liên tục.<br>**S=3:** Không crash nhưng latency p95 tăng vọt khi tải cao, dẫn đến timeout giao dịch.<br>**B=4:** Bottleneck trên hot path thanh toán đe dọa SLO Checkout ≥99.0% khi flash sale.                                  | Low cost.                       | Approved      |
| CDO08-REL-11 |   3 |   4 |   4 | **48** | P1       | **L=3:** Thảm họa dữ liệu ít thường xuyên nhưng khi xảy ra là không thể tự phục hồi.<br>**S=4:** Không có runbook DR đồng nghĩa với không biết cách khôi phục, RTO/RPO vô hạn.<br>**B=4:** Vi phạm cam kết liên tục hoạt động kinh doanh; không thể restore cart/order đang chạy.                        | Needs CDO04 Review.             | Approved      |
| CDO08-REL-12 |   3 |   3 |   3 | **27** | P2       | **L=3:** EKS node drain xảy ra khi bảo trì hoặc nâng cấp định kỳ.<br>**S=3:** Pod bị evict đồng thời gây downtime ngắn, nhưng hồi phục sau vài chục giây.<br>**B=3:** Vi phạm SLO khi bảo trì, nhưng window ảnh hưởng hẹp và ít thường xuyên. Cần xử lý sau khi replica ≥2.                              | Low cost.                       | Approved      |
| CDO08-SEC-01 |   4 |   4 |   3 | **48** | P1       | **L=4:** Credentials nằm plain-text trong Git — ai có quyền đọc repo đều lấy được bất kỳ lúc nào.<br>**S=4:** Attacker có DB password = đọc/xóa/thay đổi toàn bộ dữ liệu production.<br>**B=3:** Không làm sập checkout ngay lập tức nhưng rò rỉ thông tin khách hàng và dữ liệu đơn hàng.               | Low cost.                       | Approved      |
| CDO08-SEC-02 |   3 |   4 |   3 | **36** | P1       | **L=3:** Chỉ xảy ra khi Grafana bị expose qua public ALB (phụ thuộc SEC-11).<br>**S=4:** Anonymous Admin có thể xóa dashboard, tắt alert, trích xuất datasource credentials kết nối DB.<br>**B=3:** Mất khả năng giám sát SLO + nguy cơ lộ DB credentials qua Grafana UI.                                | Low cost.                       | Approved      |
| CDO08-SEC-03 |   3 |   4 |   2 | **24** | P2       | **L=3:** OpenSearch đang accessible trong cluster không cần auth.<br>**S=4:** Đọc/xóa/thay đổi logs và traces tùy ý, xóa audit trail để che hành vi xâm nhập.<br>**B=2:** Ảnh hưởng khả năng điều tra sự cố và compliance, nhưng không trực tiếp làm gián đoạn giao dịch.                                | Low cost.                       | Approved      |
| CDO08-SEC-04 |   3 |   3 |   2 | **18** | P2       | **L=3:** RCE cần điều kiện khai thác phức tạp, không thường xuyên.<br>**S=3:** Nếu có RCE, container không được hardening dễ bị escape ra node host.<br>**B=2:** Container escape cần nhiều bước tiếp theo để ảnh hưởng business — rủi ro dài hạn hơn là ngay lập tức.                                   | Low cost.                       | Approved      |
| CDO08-SEC-05 |   3 |   4 |   4 | **48** | P1       | **L=3:** Bất kỳ người dùng internet nào cũng có thể scan và truy cập public ALB.<br>**S=4:** Truy cập được Grafana/Jaeger đọc PII trong traces; flagd-ui có thể bị dùng để inject fault vào checkout.<br>**B=4:** Rò rỉ PII khách hàng + nguy cơ disrupt checkout ngay lập tức qua fault injection.      | Low cost.                       | Approved      |
| CDO08-SEC-06 |   2 |   4 |   3 | **24** | P2       | **L=2:** Cần Grafana pod bị compromise trước — bước đầu khó xảy ra nếu SEC-11 được fix.<br>**S=4:** Nếu bị chiếm, token SA đọc được toàn bộ Secrets trong mọi namespace (DB pass, AWS keys).<br>**B=3:** Blast radius rất lớn nhưng phụ thuộc vào attack chain, không xảy ra độc lập.                    | Low cost.                       | Approved      |
| CDO08-SEC-07 |   2 |   3 |   2 | **12** | P2       | **L=2:** Cần một pod bị exploit trước mới khai thác được SA chung — bước đầu ít xảy ra.<br>**S=3:** Lateral movement: pod bị chiếm có thể dùng SA để leo thang quyền sang service khác.<br>**B=2:** Rủi ro kiến trúc phân quyền dài hạn, chưa có impact trực tiếp hiện tại.                              | Low cost.                       | Approved      |
| CDO08-SEC-08 |   2 |   2 |   1 |  **4** | P3       | **L=2:** Attacker cần viết script browser và có người dùng chạy trên domain để exploit.<br>**S=2:** Gây noise trong telemetry, không trực tiếp tổn hại hệ thống.<br>**B=1:** Không ảnh hưởng đến dữ liệu người dùng, SLO, hay doanh thu — chỉ là hygiene observability.                                  | Low cost.                       | Approved      |

---

## Week 2-3 Order

| Order                      | Items                                                                                                                          | Reason                                                                                                                                                                                                                                                   |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Do first (P0)**          | `CDO08-REL-01`, `CDO08-REL-02`, `CDO08-REL-09`, `CDO08-REL-03`, `CDO08-REL-07`                                                 | Trực tiếp tháo gỡ điểm chết đơn lẻ (SPOF), chống treo hệ thống và bảo vệ SLO luồng Checkout (bao gồm cả giải quyết lỗi không nhất quán dữ liệu thanh toán). Đây là những luồng ảnh hưởng trực tiếp đến doanh thu và trải nghiệm đặt hàng của khách hàng. |
| **Do next (P1)**           | `CDO08-REL-10`, `CDO08-REL-11`, `CDO08-SEC-05`, `CDO08-SEC-01`, `CDO08-SEC-02`, `CDO08-REL-06`                                 | Các item cải thiện hiệu năng (payment OpenFeature init), an toàn thông tin (secret rotation), ngăn ngừa xâm nhập bên ngoài (admin routes exposure) và cảnh báo vận hành (alertmanager).                                                                  |
| **Track / verify (P2/P3)** | `CDO08-REL-12`, `CDO08-SEC-06`, `CDO08-SEC-03`, `CDO08-SEC-04`, `CDO08-REL-05`, `CDO08-REL-08`, `CDO08-SEC-07`, `CDO08-SEC-08` | Các hạng mục mang tính chất hygiene (security context, shared SA, CORS, ClusterRole) hoặc cấu hình đồng bộ test của BTC (`REL-08` flagd sync), không trực tiếp tác động đến giao dịch mua sắm của khách hàng.                                            |

---

## Definition Of Ready For Week 2 Implementation

Một backlog item chỉ nên chuyển sang triển khai khi có đủ:

- Evidence hiện tại có timestamp.
- Owner chính và owner phối hợp.
- Service/file bị ảnh hưởng.
- Cách verify sau khi sửa.
- Rollback hoặc mitigation nếu thay đổi gây lỗi.
- Priority đã được PM chấm lại bằng `pm-priority-rubric.md`.
