# Jaeger Screenshot Placeholder

Place PERF-04.4 screenshots here:

- `checkout-trace.png`
- `product-flow-trace.png`

Current status:

- Jaeger public route `/jaeger/ui/` returns `HTTP 200 OK`.
- Screenshot capture is pending because browser screenshot capture was not available in the current automation session.

