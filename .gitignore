# ── Claude Code ──
.claude/worktrees/
.agents/worktrees/

# ── IDE / Editor ──
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# ── OS ──
*.lnk

# ── Go ──
*.exe
*.test
*.out
vendor/
go.work

# ── Java ──
*.class
*.jar
*.war
target/
.gradle/
build/

# ── C# / .NET ──
bin/
obj/
*.user
*.suo
*.cachefile
*.userosscache
*.userprefs
*.DotSettings.user
/packages/
*.nupkg

# ── Node / TypeScript ──
node_modules/
.next/
dist/
*.tsbuildinfo
.env.local
.env.*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Python ──
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# ── Docker ──
*.tar
.dockerignore_local

# ── Helm / Kubernetes ──
charts/*.tgz

# ── Environment / Secrets ──
.env
.env.*
!.env.override
*.pem
*.key
credentials.*
secrets.yaml
secrets.yml

# ── Terraform / IaC (nếu có sau này) ──
.terraform/
*.tfstate
*.tfstate.*

# ── Logs ──
*.log
logs/

# ── Temp / Cache ──
tmp/
temp/
.cache/

# ── User Note ──
note/

# ── Tracked lockfiles (allow specificity overrides above) ──
!infra/bootstrap/.terraform.lock.hcl
!infra/terraform/.terraform.lock.hcl
!techx-corp-chart/Chart.lock

# ── Local Scan Guide ──
huong-dan-grafana.md
docs/aio01/w1_tasks_summary.md
docs/aio01/w2_tasks_summary.md

# ── Generated Manifests ──
techx-corp-app.yaml

