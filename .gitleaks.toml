# File cấu hình Gitleaks mở rộng cấu hình mặc định (Extend)
# Quyết định bởi: Tech Lead Quyết (CDO08)
# Trạng thái: Hoạt động

[extend]
useDefault = true

[allowlist]
description = "Ngoại lệ được phê duyệt cho mock keys trong unit tests"
paths = [
    # Bỏ qua các mock keys phục vụ cho kiểm thử của app product-reviews
    '''techx-corp-platform/src/product-reviews/tests/.*'''
]

commits = ["1ee3e8abbb0a53a772a2c0fe56f7886e397aa332"]
